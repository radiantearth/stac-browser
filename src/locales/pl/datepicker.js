const dateFormat = 'YYYY-MM-DD';
const timeFormat = 'HH:mm:ss';
const dateTimeFormat = `${dateFormat} ${timeFormat}`;
const locale = require('vue2-datepicker/locale/pl');
export default {dateFormat, timeFormat, dateTimeFormat, locale};
